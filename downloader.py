import time\nfrom selenium import webdriver\nfrom selenium.webdriver.common.by import By\nfrom selenium.webdriver.chrome.service import Service as ChromeService\nfrom webdriver_manager.chrome import ChromeDriverManager\n\ndef download_files(playlist_url):\n    # Set up Selenium with ChromeDriver\n    driver = webdriver.Chrome(service=ChromeService(ChromeDriverManager().install()))\n    driver.get(playlist_url)\n\n    # Wait for the page to load\n    time.sleep(5)\n\n    # Locate and download .wav files and meta.json\n    wav_files = driver.find_elements(By.XPATH, '//a[contains(@href, ".wav")]')\n    for wav in wav_files:\n        wav_url = wav.get_attribute('href')\n        print(f'Downloading {wav_url}')\n        # Add your code to download the file here\n\n    meta_file = driver.find_element(By.XPATH, '//a[contains(@href, "meta.json")]')\n    meta_url = meta_file.get_attribute('href')\n    print(f'Downloading {meta_url}')\n    # Add your code to download meta.json here\n\n    # Close the driver\n    driver.quit()\n\n# Example usage\n# download_files('https://example.com/your-playlist')\n